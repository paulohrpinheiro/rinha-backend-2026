volumes:
  sock:
    driver: local

services:
  proxy:
    image: paulohrpinheiro/rinha-proxy:v50
    ports:
      - "9999:9999"
    environment:
      - BACKENDS=http://api-1:8080,http://api-2:8080
      - PORT=9999
      - UNIX_SOCKET_DIR=/run/sock
    depends_on:
      api-1:
        condition: service_started
      api-2:
        condition: service_started
    networks:
      - rinha-net
    volumes:
      - sock:/run/sock
    deploy:
      resources:
        limits:
          cpus: "0.19"
          memory: "20MB"

  api-1:
    hostname: api-1
    image: paulohrpinheiro/rinha-api:v50
    environment:
      - PORT=8080
      - RESOURCES_DIR=/resources
      - UNIX_SOCKET_DIR=/run/sock
      - GOGC=off
      - GOMEMLIMIT=150MiB
    networks:
      - rinha-net
    volumes:
      - sock:/run/sock
    deploy:
      resources:
        limits:
          cpus: "0.405"
          memory: "165MB"

  api-2:
    hostname: api-2
    image: paulohrpinheiro/rinha-api:v50
    environment:
      - PORT=8080
      - RESOURCES_DIR=/resources
      - UNIX_SOCKET_DIR=/run/sock
      - GOGC=off
      - GOMEMLIMIT=150MiB
    networks:
      - rinha-net
    volumes:
      - sock:/run/sock
    deploy:
      resources:
        limits:
          cpus: "0.405"
          memory: "165MB"

# warmup removed: runs inline in the API binary (handler.Warmup())

networks:
  rinha-net:
    driver: bridge
